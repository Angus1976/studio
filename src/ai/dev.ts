import { config } from 'dotenv';
config();

// The following imports are commented out because the 'genkit' dependency
// is not currently installed in the project. This is to prevent build errors.
// Refer to CONFIGURATION_README.md for more details on how to re-enable AI features.

// import '@/ai/flows/ai-requirements-navigator.ts';
// import '@/ai/flows/prompt-library-connector.ts';
// import '@/ai/flows/digital-employee.ts';
